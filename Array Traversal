Create a static array of any size and print all element.
#include <stdio.h>

int main()
{
   int arr[5]={4,3,2,1,5};
   for(int i=0;i<=4;i++)
   {
       printf("%d\n",arr[i]);
   }
}


Dynamic Array :-

#include <stdio.h>
#include<stdlib.h>
int main()
{
  int n;
  printf("enter the size of array: ");
  scanf("%d",&n);
  int*arr = (int*)malloc(n* sizeof(int));
  for(int i=0;i<n;i++)
  {
      printf("enter the value at arr[%d]index: ",i);
      scanf("%d",&arr[i]);
  }
  
        printf("the value at array: ");
   for(int i=0;i<n;i++)
{
              printf("%d\n",arr[i]);
              
          }
  
}
calloc






