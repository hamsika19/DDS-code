#include <iostream>
using namespace std;
int main() 
{
int n;
cout<<"Enter the size of array :";
cin>>n;
int *arr = new int[n];
for(int i = 0; i<n; i++)
{
    cout<<"Enter the value at arr["<<i<<"]index";
    cin>>arr[i];
}
  cout<<endl<<"entered elements are :"<<endl;
  for(int i = 0; i<n; i++)
  {
      cout<<arr[i]<<endl;
  }

int n1;
cout<<"enter the new size of array :";
cin>>n1;
int *arr1 = new int[n1];
for(int i=0; i<n1; i++)
{
    arr[i]=arr[i];
}
if(n1 > n)
{
    for(int i=n; i<n1; i++)
    {
        cout<<"enetr the element at arr["<<i<<"]index";
        cin>>arr[n1];
    }
}
}
